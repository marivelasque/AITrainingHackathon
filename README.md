# code for build a claw course

## Furniture Shop Buyer App

Flask app, buyer-facing catalogue/orders against a real event API. See `CLAUDE.md` for how
to run it, `requirements.md` for what it does, `architecture.md` for how it's built.
